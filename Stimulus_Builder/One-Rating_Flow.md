one trial is rated like this:

## 1.
run_ratings.py loads all *_design.json files from design_out_v2/:
run_ratings.py (line 141), run_ratings.py (line 154)

## 2.
It reads participant/trials from your design file (e.g. participant_type=TYPE_E_HIGH, trial 1 with output_id=3371...):
PER_EHIGH_0001_design.json (line 3), PER_EHIGH_0001_design.json (line 28), PER_EHIGH_0001_design.json (line 35)

## 3.
It picks rating model + temperature from CLI args (--model, --temperature; default temp is 0.0):
run_ratings.py (line 90), run_ratings.py (line 101)

## 4.
For each trial, it calls rate_ad_structured(...) with the trial message, participant type, selected model, and selected temperature:
run_ratings.py (line 198), run_ratings.py (line 205)

## 5.
Persona prompt is built at runtime in llm_interface.py:

- It loads persona text from persona_templates/TYPE_E_HIGH. json or TYPE_O_HIGH.json
llm_interface.py (line 10), llm_interface.py (line 77)

- It composes a user prompt like “Rate the ad as this participant…”
llm_interface.py (line 132), llm_interface.py (line 150)

## 6.
That prompt is sent to the chosen rating model via chat.completions.create(...) with temperature=...:
llm_interface.py (line 191), llm_interface.py (line 193)

Important clarification:
in the active script pipeline, persona is not generated by a separate model call each run. It is loaded from the template JSON files and inserted into the prompt.